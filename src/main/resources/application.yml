azure:
  service-bus:
    connection-string: sensitive_set_get_from_env_var
endpoints:
  enabled: true
  sensitive: false
info:
  java:
    source: "${java.version}"
logging:
  level:
    root: INFO
management:
  info:
    build:
      enabled: true
    git:
      mode: full
server:
  port: 8097
  context-path: /candidate
spring:
  application:
    name: Candidate Service
  data:
    mongodb:
      database: candidate
      host: localhost
      port: 27017
---
server:
  port: 8080
spring:
  data:
    mongodb:
      host: mongodb
  profiles: kub-aks
